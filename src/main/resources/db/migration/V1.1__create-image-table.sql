CREATE TABLE IF NOT EXISTS `image` (
                                       `id` BIGINT NOT NULL AUTO_INCREMENT,
                                       `file_data` LONGBLOB NOT NULL,
                                       PRIMARY KEY (`id`)
    );
